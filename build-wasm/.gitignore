# Large build inputs — fetched via fetch-statics.sh, never committed.
/statics/
# Local hermetic .NET SDK (install via dotnet-install.sh; see BUILD-WASM.md).
/.toolchain/
# Build outputs.
**/bin/
**/obj/
/dist/
*.zip
/build-wasm/*.png
*.png
# ...but shipped page assets are real sources (negation must follow the blanket ignore)
!loader/wwwroot/*.png
/.uo-test-data/
/.run/
